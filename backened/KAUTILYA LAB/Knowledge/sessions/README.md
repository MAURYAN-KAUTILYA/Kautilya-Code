# Sessions

Saved session records live here.

## What Lives Here

- Per-session JSON files with message history, summary data, keys, commands, and sketch state

## Why It Matters

Sessions make the builder resumable. The app can reopen context, continue a thread, and keep the user's work connected.
