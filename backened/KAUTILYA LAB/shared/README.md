# Shared

Shared command definitions live here.

## What Lives Here

- `kautilya-commands.ts` - slash commands, directives, tiers, and command reference building

## Why It Matters

The frontend and backend both depend on the same command language. That keeps the command sheet, the parser, and the orchestration layer aligned.
