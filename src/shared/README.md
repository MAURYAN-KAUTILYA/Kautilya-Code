# Shared

Shared frontend command types and helpers live here.

## What Lives Here

- Slash-command definitions
- Command resolution helpers
- Shared typing for the command language

## Why It Matters

This folder keeps the app speaking one language everywhere. The builder, the command sheet, and the backend pipeline all stay aligned.
