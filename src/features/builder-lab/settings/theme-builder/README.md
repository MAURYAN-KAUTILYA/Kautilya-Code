# Theme Builder

Theme customization lives here.

## What Lives Here

- `ThemeBuilderFeature.tsx` - theme mode and accent selector
- `index.ts` - feature exports

## Why It Matters

This module keeps the UI polished without feeling locked. Users can tune the accent mood while the overall product language stays coherent.
